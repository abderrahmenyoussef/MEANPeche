import { TunisianCurrencyPipe } from './tunisian-currency.pipe';

describe('TunisianCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new TunisianCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
